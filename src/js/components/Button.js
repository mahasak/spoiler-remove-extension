import React from 'react';

class Button extends React.Component {
    render() {
        return (
            <button>
                Hello
      </button>
        )
    }
}

Button.propTypes = {
};

export default Button;